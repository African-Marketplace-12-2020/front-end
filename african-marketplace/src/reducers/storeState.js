import { 
    FETCH_START, 
} from '../actions/marketActions';

const initialState = {
    data: ''
}

export const storeState = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state
    }
}