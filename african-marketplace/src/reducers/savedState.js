import { 
    FETCH_START, 
} from '../actions/marketActions';

const initialState = {
    data: ''
}

export default function (state = initialState, action) {
    switch(action.type) {
        default: 
            return state
    }
}