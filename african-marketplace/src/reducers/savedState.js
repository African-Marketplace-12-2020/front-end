import { 
    FETCH_START, 
} from '../actions/marketActions';

const initialState = {
    data: ''
}

export const savedState = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state
    }
}